# 游戏部署经验总结（v0.5.1）

## 一、各平台部署对比

| 平台 | 国内速度 | 费用 | 实名认证 | 自动部署 | 部署难度 | 状态 |
|------|----------|------|----------|----------|----------|------|
| **GitHub Pages** | 需代理 | 免费 | 不需要 | Git推送自动 | 简单 | ✅ **当前主用** |
| Netlify | 偏慢 | 免费 | 不需要 | 支持Git | 简单 | ✅ 备用 |
| Vercel | 中等 | 免费 | 不需要 | 支持Git | 中等 | ❌ CLI上传失败 |
| Gitee Pages | 快 | 免费 | 需要 | 需手动更新 | 困难 | ❌ 需实名认证 |
| Cloudflare Pages | 中等 | 免费 | 不需要 | 支持Git | 中等 | 未尝试 |
| 阿里云OSS/腾讯云COS | 快 | 几元/月 | 需要备案 | 支持API | 中等 | 未尝试 |

## 二、GitHub Pages 部署（当前主用，推荐）

### 部署地址
- **生产地址**：https://wsjshy.github.io/quanzhi-fashi-game/
- **仓库地址**：https://github.com/wsjshy/quanzhi-fashi-game
- **部署方式**：Git推送自动部署，无需手动操作

### 完整部署流程

#### 1. 创建 GitHub 仓库
- 登录 https://github.com/new
- Repository name: `quanzhi-fashi-game`
- Description: `基于小说《全职法师》的HTML5网页游戏，纯前端实现，双击即玩。包含魔法战斗、修炼升级、探索冒险、NPC对话、任务系统、随机事件、大事件剧情等丰富玩法。`
- 选择 **Public**（GitHub Pages 免费版需要公开仓库）
- **不要**勾选 Add README、Add .gitignore、Add license（项目已有这些文件）
- 点击 Create repository

#### 2. 配置 Git 代理（中国大陆必须）
GitHub 在中国大陆直连不稳定，Git 命令行需要配置系统代理：
```powershell
# 查看系统代理（Clash/V2Ray等通常是 127.0.0.1:7890）
Get-ItemProperty -Path "HKCU:\Software\Microsoft\Windows\CurrentVersion\Internet Settings" | Select-Object ProxyEnable, ProxyServer

# 为 GitHub 设置 Git 代理（关键步骤！）
& "D:\Git\bin\git.exe" config --global http.https://github.com.proxy http://127.0.0.1:7890
```

**注意**：浏览器能访问 GitHub 不代表 Git 命令行能访问。浏览器自动走系统代理，但 Git 需要单独配置。

#### 3. 添加远程仓库并推送
```powershell
cd C:\Users\22210\Desktop\quanzhi-fashi-game-master

# 添加 GitHub 远程仓库（如果还没添加）
& "D:\Git\bin\git.exe" remote add github https://github.com/wsjshy/quanzhi-fashi-game.git

# 推送代码
& "D:\Git\bin\git.exe" push -u github master
```

#### 4. 启用 GitHub Pages
- 进入仓库 Settings → Pages（或直接访问 https://github.com/wsjshy/quanzhi-fashi-game/settings/pages）
- Source: 保持 "Deploy from a branch"
- Branch: 选择 `master`，文件夹保持 `/ (root)`
- 点击 Save
- 等待 1-2 分钟部署完成
- 访问地址：https://wsjshy.github.io/quanzhi-fashi-game/

#### 5. 后续更新
每次开发完成后，正常 git commit + push 即可自动部署：
```powershell
& "D:\Git\bin\git.exe" add .
& "D:\Git\bin\git.exe" commit -m "类型：描述"
& "D:\Git\bin\git.exe" push github master
```
推送后 1-2 分钟自动生效，无需手动操作。

### GitHub Pages 注意事项
1. **必须公开仓库**：免费版 GitHub Pages 不支持私有仓库
2. **分支名**：项目用 master 分支，GitHub 新默认是 main，不影响，选 master 即可
3. **根目录部署**：游戏入口 index.html 在根目录，不需要 /docs 子目录
4. **HTTPS 强制开启**：Settings → Pages 底部有 Enforce HTTPS 选项，建议开启
5. **404 问题**：刚保存设置后访问会 404，等 1-2 分钟部署完成即可
6. **代理只影响 Git**：部署后的网站用户访问不需要代理（但国内直连 GitHub Pages 速度可能较慢）
7. **相对路径**：游戏中所有资源引用使用相对路径，不要用绝对路径

### 浏览器自动化操作 GitHub 的经验
GitHub 网页上的按钮和下拉菜单用自动化工具点击时经常不响应，解决方案：
- **普通 click 不生效时**：使用 `mouse_down` + `mouse_up` 组合模拟完整点击
- **下拉菜单**：同样需要 mouse_down + mouse_up 才能展开
- **表单提交**：填完表单后也需要 mouse_down + mouse_up 点击提交按钮
- **Tab 键导航**：在 GitHub 表单中 Tab 键焦点移动可能不可见，不推荐
- **直接导航 URL**：Settings 页面可以直接访问 `/settings/pages`，不需要点击导航栏

## 三、Netlify 部署（备用）

### 部署地址
- 生产地址：https://quanzhi-fashi-game.netlify.app
- 站点ID：29cad1c9-36ed-48fd-854f-2c0052e01ca0
- 管理后台：https://app.netlify.com/projects/quanzhi-fashi-game

### 关键踩坑：必须创建干净部署目录
**错误做法**：直接部署项目根目录
```
netlify deploy --prod --dir=.  # 会报错 JSONHTTPError: no records matched 422
```
**原因**：项目根目录包含 `.git/`、`docs/`、`tests/`、`tools/` 等非游戏文件。

**正确做法**：
```powershell
New-Item -ItemType Directory -Path deploy_dist
Copy-Item index.html deploy_dist
Copy-Item -Recurse engine deploy_dist
Copy-Item -Recurse assets deploy_dist
netlify deploy --prod --dir=deploy_dist --site=<site_id>
Remove-Item -Recurse -Force deploy_dist
```

### 一键部署脚本
```powershell
cd C:\Users\22210\Desktop\quanzhi-fashi-game-master
.\tools\deploy.ps1
```

## 四、Gitee 部署（国内最快，但需实名认证）

### 仓库地址
- https://gitee.com/wsjshy/quanzhi-fashi-game.git
- 代码已推送成功

### 问题
- Gitee Pages 需要实名认证后才能使用
- 每次推送后需要手动点击"更新"才会重新部署
- 实名认证入口可能在右上角头像菜单中

## 五、Vercel 部署（失败记录）

### 失败原因
从国内 CLI 上传文件到 Vercel 服务器网络不稳定，连续4次失败：
```
Error: fetch failed
AbortError: This operation was aborted
```

### 可能的替代方案
1. 连接 GitHub 仓库自动部署（Vercel 从 GitHub 拉取代码，绕过本地上传）
2. Vercel 网页直接拖拽上传

## 六、手机端适配经验

### 适配方案
CSS 媒体查询 + class 标记，仅屏幕宽度 < 768px 时触发手机布局。

### 关键 CSS（index.html）
```css
@media (max-width: 768px) {
    .mobile-main-content { flex-direction: column !important; }
    .mobile-action-panel { flex: none !important; width: 100% !important; }
    .mobile-side-menu { width: 100% !important; border-left: none !important; border-top: 2px solid #444477 !important; }
    .mobile-top-bar { flex-wrap: wrap !important; gap: 8px !important; }
    .mobile-element-grid { grid-template-columns: repeat(2, 1fr) !important; }
    .mobile-popup { max-width: 92vw !important; width: 92vw !important; }
    .mobile-title h1 { font-size: 32px !important; }
    body { overflow-x: hidden; }
}
```

### 注意事项
- 内联样式优先级高于 CSS，必须用 `!important` 覆盖
- viewport meta 标签必须有：`<meta name="viewport" content="width=device-width, initial-scale=1.0">`
- 游戏存档存在浏览器 localStorage，不同设备/浏览器不互通

## 七、命令速查

### Git 相关（PowerShell）
```powershell
# 完整路径
& "D:\Git\bin\git.exe" <command>

# 常用操作
& "D:\Git\bin\git.exe" status
& "D:\Git\bin\git.exe" add .
& "D:\Git\bin\git.exe" commit -m "类型：描述"
& "D:\Git\bin\git.exe" push github master
& "D:\Git\bin\git.exe" push gitee master

# 代理设置
& "D:\Git\bin\git.exe" config --global http.https://github.com.proxy http://127.0.0.1:7890

# 语法检查
node --check engine/<file>.js
```

### 远程仓库
```powershell
# 查看远程
& "D:\Git\bin\git.exe" remote -v

# 添加远程
& "D:\Git\bin\git.exe" remote add github https://github.com/wsjshy/quanzhi-fashi-game.git
& "D:\Git\bin\git.exe" remote add gitee https://gitee.com/wsjshy/quanzhi-fashi-game.git

# 同时推送到多个远程
& "D:\Git\bin\git.exe" push github master; & "D:\Git\bin\git.exe" push gitee master
```

## 八、临时文件清理

部署过程中产生的临时文件：
- `deploy_dist/` - Netlify 干净部署目录（deploy.ps1 自动清理）
- `deploy_log*.txt` - 部署日志
- `netlify_*.txt` - Netlify 输出
- `github_form.png` - 截图临时文件

## 九、推荐部署方案

**当前方案**：GitHub Pages（主） + Gitee（备用，需实名认证后启用）
- 开发 → git commit → git push github master → 自动部署
- 国内访问 GitHub Pages 速度可能一般，但稳定可靠，无需手动操作
- 如果需要国内加速，后续可考虑：
  1. 完成 Gitee 实名认证启用 Gitee Pages
  2. 使用 Cloudflare CDN 加速
  3. 购买云服务器 + 域名（需备案）
